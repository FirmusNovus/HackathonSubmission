export interface EbsiUriComponents<Scheme extends string = string, Network extends string = string, Service extends string = string> {
    network: Network;
    resource: string;
    scheme: Scheme;
    service: Service;
}
export interface EbsiUriConfiguration<Scheme extends string = string, Network extends string = string, Service extends string = string> {
    network: {
        isOptional?: boolean;
        name: Network;
    };
    scheme: Scheme;
    services?: SupportedServices<Service>;
}
export type ServiceVersion = `v${number}`;
export type SupportedServices<Service extends string> = Record<Service, ServiceVersion>;
export declare function fromUrl<Scheme extends string, Network extends string, Service extends string>(url: string, config: EbsiUriConfiguration<Scheme, Network, Service>): `${Scheme}:${string}`;
export declare function parse<Scheme extends string, Network extends string, Service extends string>(uriString: string, config: EbsiUriConfiguration<Scheme, Network, Service>): EbsiUriComponents<Scheme, Network, Service>;
export declare function parseUrl<Scheme extends string, Network extends string, Service extends string>(url: string, config: EbsiUriConfiguration<Scheme, Network, Service>): EbsiUriComponents<Scheme, Network, Service>;
export declare function serialize<Scheme extends string, Network extends string, Service extends string>(components: EbsiUriComponents<Scheme, Network, Service>, config: EbsiUriConfiguration<Scheme, Network, Service>): `${Scheme}:${string}`;
export declare function toUrl<Scheme extends string, Network extends string, Service extends string>(uriString: string, host: string, config: EbsiUriConfiguration<Scheme, Network, Service>): string;
//# sourceMappingURL=index.d.ts.map